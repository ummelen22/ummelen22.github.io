[workspace]
authors = ["Alex Ummelen <aummelen@smart-robotics.nl>"]
channels = ["conda-forge"]
name = "daughters-of-the-himalaya-landing"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]
install = "npm install"
dev = "npm run dev"
build = "npm run build"
build-dev = "npm run build:dev"
lint = "npm run lint"
preview = "npm run preview"

[dependencies]
nodejs = ">=20"
